Быстрый рекап: где мы вчера остановились

✅ Что было сделано

- Graceful shutdown — добавили во все сервисы (кроме Auth, но это быстро)
- Proto — убрали поле score из SubmitScoreRequest (сервер сам считает)
- Валидатор — полноценная эмуляция игры на сервере:
- Восстановление доски по seed
- Применение всех ходов из moves
- Честный подсчёт очков через CalculateScore (группы плиток)

Тест — успешно прошёл:

grpcurl → Game → валидация → подсчёт 4 очков → NATS → Leaderboard