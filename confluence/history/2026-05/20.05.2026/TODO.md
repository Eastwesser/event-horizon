# TODO — 20 мая 2026

## 🔴 Критично

### 1. Починить суммирование очков в лидерборде
- [ ] Проверить логику `UpdateScore` в leaderboard сервисе
- [ ] Нужно накапливать общий счёт, а не заменять
- [ ] Формула: новое очки = старый рекорд + новый рекорд? Или только максимальный?

### 2. Починить `userId` в localStorage
- [ ] Проверить, что после логина `userId` сохраняется
- [ ] Добавить восстановление из токена
- [ ] Добавить fallback

### 3. WebSocket — держать соединение постоянно
- [ ] Создавать WebSocket при монтировании `Leaderboard` компонента
- [ ] Не закрывать при закрытии модального окна
- [ ] Обновлять таблицу при каждом `score.updated`

## 🟡 Важно

### 4. Вернуть комментарии в код
- [ ] Восстановить поясняющие комментарии в `gateway/cmd/main.go`
- [ ] Особенно в местах с WebSocket, NATS, gRPC

### 5. HTTP fallback для лидерборда
- [ ] Если WebSocket закрыт — использовать `GET /api/leaderboard`
- [ ] При открытии модального окна всегда запрашивать свежий топ

## 🟢 Приятно

### 6. Улучшить UI/UX
- [ ] Показывать загрузку (spinner) при запросе
- [ ] Обрабатывать ошибки соединения
- [ ] Анимация обновления таблицы

## Команды для быстрого старта

```bash
# Бэкенд
cd ~/event_horizon
make all

# Фронтенд
cd frontend
npm run dev

# Проверить NATS
nats sub "score.updated" --server localhost:4222

# Проверить лидерборд через HTTP
curl "http://localhost:8080/api/leaderboard?game_id=hexagon&limit=10"