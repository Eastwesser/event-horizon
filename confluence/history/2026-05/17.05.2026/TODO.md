# TODO — 17 мая 2026

## 🔴 Критично (баги)

### 1. Починить отправку рекорда
- [ ] Проверить `localStorage.getItem('userId')` в `submitScore`
- [ ] Убедиться, что `userId` сохраняется при логине/регистрации
- [ ] В `Login.tsx` добавить сохранение `userId` из ответа сервера
- [ ] В `Register.tsx` добавить сохранение `userId`

### 2. Починить лидерборд
- [ ] Сделать WebSocket соединение постоянным (не только при открытом модальном окне)
- [ ] Убедиться, что бэкенд публикует `score.updated` после `submitScore`
- [ ] Проверить, что leaderboard подписан на `score.updated`

## 🟡 Важно

### 3. Выбор уровня сложности
- [ ] Создать компонент `LevelSelector` перед началом игры
- [ ] Передавать выбранный уровень в `initGame(level)`
- [ ] Сохранять выбранный уровень в store

### 4. Анимация исчезновения стопки
- [ ] Добавить CSS-анимацию "ам-ам-ам" при `checkAndClearStack`
- [ ] Длительность 1-2 секунды

## 🟢 Приятно

### 5. Пауза в игре
- [ ] Кнопка "Пауза" (останавливает таймеры и приём ходов)
- [ ] Возможность продолжить

## Команды для отладки

```bash
# Логи game (отправка рекорда)
tail -f /tmp/game.log | grep -E "Published|submit|userId"

# Логи leaderboard (получение рекорда)
tail -f /tmp/leaderboard.log | grep -E "Received|score"

# Проверить userId в localStorage (в консоли браузера)
console.log(localStorage.getItem('userId'));

Приоритет на завтра

🔴 Починить user=undefined (1 час)
🔴 Починить лидерборд (1 час)
🟡 Выбор уровня сложности (2 часа)
