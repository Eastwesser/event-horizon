# TODO — 25 мая 2026

## 🔴 Критично

### 1. Починить накопление очков в Game сервисе
**Проблема:** Game публикует `score=0`, хотя игрок набрал 109+ очков.

**Нужно проверить:**
- [ ] Поле `Count` в `Tile` инициализируется правильно?
- [ ] `MoveTile` корректно обновляет `TotalScore`?
- [ ] `CalculateScore` возвращает накопленный счёт?
- [ ] Добавить отладочные логи в `MoveTile` (каждое объединение, каждое очищение)

**Место:** `services/game/games/hexagons/board.go`

### 2. Починить сохранение `userId` в localStorage
**Проблема:** `localStorage.getItem('userId')` возвращает `undefined` после логина.

**Нужно:**
- [ ] Убедиться, что `Login.tsx` правильно сохраняет `user_id`
- [ ] Добавить восстановление из токена при загрузке страницы
- [ ] Проверить, что `user_id` приходит от сервера (curl показывает, что приходит)

### 3. Leaderboard должен суммировать очки
**Проблема:** Redis хранит только последний счёт, а не сумму.

**Нужно:**
- [ ] Проверить `UpdateScore` в `redis_repo.go` — суммирование уже добавлено
- [ ] Убедиться, что Game публикует правильный `score`

## 🟡 Важно

### 4. WebSocket — постоянное соединение
- [ ] Не закрывать при закрытии модального окна
- [ ] Обновлять таблицу при каждом `score.updated`

### 5. Комментарии в коде
- [ ] Вернуть поясняющие комментарии (особенно в `gateway/cmd/main.go`)

## 🟢 Приятно

### 6. UI/UX
- [ ] Показывать загрузку при запросе
- [ ] Обрабатывать ошибки соединения
- [ ] Анимация обновления таблицы

## Диагностические команды

```bash
# Смотреть, что публикует Game
nats sub "score.updated" --server localhost:4222

# Логи Game с отладочными принтами
tail -f /tmp/game.log

# Проверить Redis напрямую
docker exec -it event-horizon-redis-leaderboard redis-cli
> ZRANGE leaderboard:hexagon 0 -1 WITHSCORES

# Проверить, что приходит в leaderboard
tail -f /tmp/leaderboard.log

Гипотеза

Вероятнее всего, проблема в SetTile: при создании плитки поле Count не инициализируется. Нужно добавить Count: 1 для новой плитки и сохранять Count при перемещении.

Завтрашний план

Добавить подробные логи в MoveTile (каждый ход, каждое объединение)
Проверить, что Count сохраняется при перемещении
Убедиться, что TotalScore увеличивается при очистке стопки (≥10)
После исправления — протестировать с одной партией
Затем починить userId и WebSocket