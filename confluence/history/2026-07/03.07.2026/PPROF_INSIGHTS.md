🧠 Анализ CPU профиля (под нагрузкой)

Что жрёт CPU:

    syscall.Syscall6 (30ms, 33%) — системные вызовы. Нормально для сетевых операций.

    runtime.futex (20ms, 22%) — синхронизация горутин (ожидание мьютексов).

    hashLabelValues (10ms, 11%) — хеширование метрик Prometheus.

    SubmitScore (20ms) — ваша бизнес-логика.

    gRPC + OpenTelemetry — тоже нагружают.



Вывод: CPU нагрузка равномерно распределена между:

    Системными вызовами (сеть)

    Синхронизацией горутин

    Бизнес-логикой

    Метриками и трейсингом

Узких мест нет. Это хороший знак!





🧠 Анализ Heap профиля (память)
Что занимает память:

    compress/flate (1.8 MB, 35%) — сжатие для Prometheus / gRPC.

    pprof.StartCPUProfile (1.18 MB, 23%) — буфер для профилирования (нормально).

    bufio.NewReaderSize (1.05 MB, 20%) — буферы для чтения.

    protobuf (0.5 MB) — парсинг protobuf.

Вывод: Память занята буферами и сжатием для сетевых операций и метрик.
Утечек не видно. Heap стабилен.

📊 Итоги нагрузочного теста (k6)

Параметр	Значение
Всего запросов	2588
RPS	~41 req/s
Средняя латентность	806 ms
Максимальная латентность	4.59 s
P90	2.29 s
P95	2.59 s
Ошибки	0% ✅
Пользователей	50 VUs

Латентность высоковата (P95 = 2.59s). Это может быть связано с:

    Медленной БД (PostgreSQL на локальной машине).

    NATS (публикация событий).

    Метриками + трейсингом (otel + prometheus).

🚀 Что можно улучшить

    Оптимизировать запросы к БД — добавить индексы, кеширование.

    Уменьшить количество метрик (если их слишком много).

    Настроить NATS (увеличить буферы, проверить производительность).

    Добавить кеш для лидерборда (уже есть Redis, но проверить, что он используется).

### 6. Результаты нагрузочного теста + pprof (03.07.2026)

#### Нагрузка: 50 VUs, 60 секунд

| Параметр | Значение |
|----------|----------|
| Всего запросов | 2588 |
| RPS | ~41 req/s |
| P95 Latency | 2.59 s |
| Ошибки | 0% |

#### CPU профиль (под нагрузкой)
- `syscall.Syscall6` (33%)
- `runtime.futex` (22%)
- `SubmitScore` (22%)
- Prometheus метрики (11%)
- gRPC + OpenTelemetry (остальное)

#### Heap профиль
- `compress/flate` (1.8 MB) — сжатие метрик
- `pprof.StartCPUProfile` (1.18 MB) — буфер профилирования
- `bufio.NewReaderSize` (1.05 MB) — буферы чтения
- **Утечек памяти не обнаружено.**

#### Рекомендации
- Оптимизировать запросы к БД
- Проверить NATS производительность
- Добавить индексы в PostgreSQL