Вот оцифрованный текст со всех предоставленных изображений, сгруппированный по блокам для удобства чтения.

### Блок 1: Вопросы по производительности и Go

**Левая часть (вопросы и грейды):**
1. Команда top показывает, что какой-то процесс потребляет 146% CPU.
2. Это реальная ситуация? Если да, то нужно ли мне что-то с этим делать?
3. 18 грейд
4. Кандидат предположил, что это может быть из-за того, что используется несколько ядер.
5. Может не знать нужно с этим что-то делать или нет.
6. 19 грейд
7. Да, абсолютно реальная. Это означает, что процесс потребляет почти 1.5 ядра.
8. При таких вводных нельзя сказать нужно ли что-то с этим делать или нет. В общем случае - это абсолютно нормально.
9. 20 грейд
10. Кандидат знает про CFS, базово может рассказать про то как система распределяет процессорное время между тасками

**Правая часть (код):**
```go
1 s := []rune("test")
2 println(s[0]) // ответ кандидата
3 s[0] = 'R'
4 println(string(s)) // ответ кандидата
```

---

### Блок 2: Задачи на написание кода (Go)

**Задача 1: Функция zip для двух слайсов**
```go
1 //Требуется реализовать функцию zip, которая соединяет элементы двух слайсов в слайс пар
2
3 func main() {
4     s1, s2 := []int{1, 2, 3}, []int{4, 5, 6, 7, 8} // может быть s1, s2 := []int{4, 5, 6, 7, 8}, []int{4, 5, 6}
5     fmt.Println(zip(s1, s2)) // [[1 4] [2 5] [3 6]]
6 }
7
8 func zip(s1 []int, s2 []int) [][]int {
9     minL := len(s1)
10    if len(s1) > len(s2) {
11        minL = len(s2)
12    }
13
14    result := make([][]int, 0, minL) // для внешнего слайса. Для внутреннего сделать нельзя
15
16    for i:=0; i<minL; i++ {
17        result = append(result, []int{s1[i], s2[i]})
18    }
19    return result
20 }
```

**Задача 2: Функция zip для произвольного количества слайсов (черновик)**
```go
1 //Требуется реализовать функцию zip, которая соединяет элементы двух слайсов в слайс пар
2 //Реализовать версию функции zip которая сможет соединять произвольное количество слайсов
3
4 func main() {
5     s1, s2 := []int{1, 2, 3}, []int{4, 5, 6, 7, 8} // может быть s1, s2 := []int{4, 5, 6, 7, 8}, []int{4, 5, 6}
6     fmt.Println(zip(s1, s2)) // [[1 4] [2 5] [3 6]]
7 }
8
9 func zip(s ...[]int) [][]int {
10    minL := len(s1)
11    if len(s1) > len(s2) {
12        minL = len(s2)
13    }
14
15    for _, v := range s {
16
17    }
18
19    result := make([][]int, 0, minL) // для внешнего слайса. Для внутреннего сделать нельзя
20
21    for i:=0; i<minL; i++ {
22        var []int
23        for _, v := range s {
24            result = append(result, )
25        }
26    }
27    return result
28 }
```

**Задача 3: Merge N каналов (Вариант 1 - с WaitGroup)**
```go
1 написать код функции, которая делает merge N каналов.
2 Весь входной поток перенаправляется в один канал.
3 ДЗ: посмотреть реализацию семафоров, в яндексе спрашивали
4 func merge(cs ...<-chan int) <-chan int {
5     result:=make(chan int)
6
7     var wg sync.WaitGroup
8     wg.Add(len(cs))
9
10    for _, ch := range cs {
11        go func() { // upper go1.22
12            defer wg.Done()
13
14            for val:= range ch {
15                result <- val
16            }
17        }()
18    }
19
20    go func() { // go func
21        wg.Wait()
22        close(result)
23    }()
24
25    return result
26 }
```

**Задача 4: Merge N каналов (Вариант 2 - упрощенный)**
```go
1 написать код функции, которая делает merge N каналов.
2 Весь входной поток перенаправляется в один канал.
3 ДЗ: посмотреть реализацию семафоров, в яндексе спрашивали
4 func merge(cs ...<-chan int) <-chan int {
5     result:=make(chan int)
6
7     var wg sync.WaitGroup
8
9     for _, ch := range cs {
10        wg.Go(func(){
11            for val:= range ch {
12                result <- val
13            }
14        }) // upper go1.22
15    }
16
17    go func() { // go func
18        wg.Wait()
19        close(result)
20    }()
21
22    return result
23 }
```

**Задача 5: Время выполнения горутин**
```go
1 // 2909 раз
2 func main() {
3     timeStart := time.Now()
4     _, _ = <-worker(), <-worker()
5     println(int(time.Since(timeStart).Seconds())) // что будет
6 }
7
8 func worker() chan int {
9     ch := make(chan int)
10    go func() {
11        time.Sleep(3 * time.Second)
12        ch <- 1
13    }()
14    return ch
15 }

// Комментарии справа:
// 3 + 3
// или 3, или 6
// Два воркера
```

---

### Блок 3: SQL Задача

**Условие:**
Дана таблица "orders":
```text
+----+---------+---------------------+-------------+
| id | user_id |     created_at      | price_total |
+----+---------+---------------------+-------------+
| 1  |   111   | 2024-01-01 10:00:00 |    2 000    |
| 2  |   222   | 2024-01-02 10:00:00 |     100     |
| 3  |   111   | 2024-04-01 10:00:00 |   20 000    |
| 4  |   222   | 2024-05-01 10:00:00 |    5 000    |
| 5  |   333   | 2024-05-02 10:00:00 |   10 000    |
+----+---------+---------------------+-------------+
```

Напишите SQL-запрос, который вернёт количество заказов по каждому пользователю с `price_total` больше или равным `1000` в таком виде отсортированному по количеству заказов в обратном порядке:

ИТОГ:
```text
+---------+-------------+
| user_id | order_count |
+---------+-------------+
|   111   |      2      |
|   222   |      1      |
|   333   |      1      |
+---------+-------------+
```

**Решение (с пометками):**
*(Пометка справа: JOIN не нужен?)*
```sql
SELECT user_id, count(*) as order_count
FROM orders
WHERE price_total >= 1000
GROUP BY user_id
ORDER BY order_count DESC
```

---

### Блок 4: Вопросы по архитектуре и масштабированию

**Ситуация:**
1 Есть два сервиса А и В. Сервис А - внешний и влиять на него мы не можем.
2 Сервис В находится полностью в вашей ответственности.
3 Бизнесовая читающая/пишущая нагрузка на ваш сервис начинает расти по ходу роста бизнеса.
4
5 Какие существуют варианты/направления для масштабирования, балансировки и оптимизации сервиса В?
6 Можно использовать любые технологии.

**Ответы (кэшик и балансировку на несколько инстансов):**
8 1. сервис долго отвечал
9
10 1. кеш
11 2. балансировка
12 3. content network delivery
13 4. вертикальное/горизонтальное масштабирование(репликация, шардирование)
14 5. CQRS.
15 6. оптимизацию: индексы + алгоритм
16 7. rate limiter
17 8. асинхронность
18 9.

**Оценка кандидата (17 грейд):**
22 17 грейд
23
24 Кандидат предложил:
25
26 клиентскую/серверную балансировку.
27 горизонтальное/вертикальное масштабирование;
28 "оптимизировать" или "порефакторить".

**Оценка кандидата (18 грейд):**
29 18 грейд
30
31 Кандидат предложил:
32
33 добавить асинхронную обработку процессов, не влияющих на бизнесовый ответ сервиса, вне запроса клиента;
34 добавить параллельность вычислений там, где это возможно;
35 посмотреть по коду "узкие места" для улучшения производительности;
36 добавить кэш;
37 посмотреть долгие запросы - попробовать накинуть индексы.
38 Кандидат знает:
39
40 варианты решений для балансировки: nginx, службы Kubernetes или иные решения.
41 возможность шардирования базы. Плюсы и минусы;
42 наличие explain операции в PSQL;
43 как обойти проблемы с гонкой за общие ресурсы у инстансов.

**Оценка кандидата (19 грейд):**
44 19 грейд
45
46 Кандидат предложил:
47
48 оптимизировать хранение:
49 запросить у PSQL долгие запросы;
50 проверить долгие запросы через EXPLAIN;
51 добавить/изменить индексы (понимает, что это, зачем, и какие +/-);
52 провести нормализацию/денормализацию данных.
53 оптимизировать или сделать асинхронными in/out операции;
54 пройти профайлеровщиком по коду, посмотреть, какие операции задерживаются от блокировок или ожидания I/O.
55 Кандидат имеет опыт балансировки.

**Оценка кандидата (20 грейд):**
58 20 грейд
59
60 Кандидат предложил:
61
62 присмотреться к архитектуре приложения. Возможно, оно реализует бизнес процессы далеко не лучшим способом;
63 проверить технологический стек. Возможно, можно перейти на более удачные готовые решения.
64 Может привести примеры из практики. (Например, замена API с JSON на gRPC, замена модулей с Питона на С или Go и т.п.)