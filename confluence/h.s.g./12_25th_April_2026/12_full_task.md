Практика:

потренировать запись и чтение из каналов

потренировать контекст и контекст с отменой и таймаутом

go
// написать синхронные запроса на главные яндекса и гугла и получить код ответа
// Усложнение: написать асинхронные запросы, применить каналы, контекст с таймаутом(defer close() - что такое close()), добавить структуру(error, url, statusCode)
http.NewRequestWithContext <- так не надо
Надо:
select {
    case <- ctx.Done():
        ...
}
*/
go
/*
GO:
1. GMP, NetPoller
2. gc
3. slice, map
4. pprof, trace
5. виды тестирования

БД:
1. транзакции, уровни изоляции транзакции?
2. индексы плюсы и минусы, b-tree, b-tree+, hash, и так далее
3. explain
4. оптимистичные и пессимистичные блокировки

Kafka:
1. at least once, at most once, exactly once
2. consumer lag
3. 7 консьюмеров и 4 партици. Что будет с консьюмерами?

Иногда спрашивают SAGA, ACID, CAP, outbox