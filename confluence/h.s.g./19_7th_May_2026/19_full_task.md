отличное видео с визуализацией про новые мапы
https://www.youtube.com/watch?v=sccdc5bTzfo&t=1247s

SIMD -  Single Instruction, Multiple Data
название той самой крутой инструкции процессора на которой оптимизация и держится
томстоун там ставится вместо удаления, это последний битик в байте
томбстоун*

Можно узнать более подробно про вычисление хеша?

а эвакуация данных тоже может быть в новой мапе, просто реже, чем в старой?

одно из свойств ещё, если большие объекты не сильно отличаются, то хэши должны отличаться сильно

про топ хэш еще спрашивают

топ хэш это же как раз тот самый мета байтик

func a() {
    x := []int{}
    x = append(x, 0) //
    x = append(x, 1) //
    x = append(x, 2) //
    y := append(x, 3) //
    z := append(x, 4) // 
    fmt.Println(y, z) // 
}

Правильная ли схема покраски? 
А -> B -> C
Сначала: 
А - белый
B - белый
С- белый
Проверяется А: 
А - серый 
В - белый 
С - белый
Проверяет В, перекрасил А: 
А - Черный 
В - серый
С- белый

вроде там был ограничение кучи (точнее достижение значение), от которой будет работать GC

--

Горутина это отдельная сущность работающая как воркер на потоках приложения? Или это и есть поток запущенный на потоке ОС?
HTTP обработчик ждет в нетполлере сигнала, а получает она его на уровне операционки из ивент пула, или нетполла?


