# БОЕВОЙ КОМПЛЕКТ КОМАНД

Мы сделаем так:

- Сначала — прогоним нагрузочный тест (k6), чтобы разогреть метрики.

- Потом — соберем все метрики через Prometheus (как настоящий SRE).

Держи полный список команд. Копируй и вставляй по порядку.

## 🚀 Фаза 1: Разогрев системы (Нагрузочный тест)
Этот тест симулирует 10 пользователей, которые непрерывно регистрируются, логинятся, играют и смотрят лидерборд в течение 30 секунд. Он создаст достаточно "шума", чтобы наши метрики ожили.

```bash
# Переходим в папку с тестами
cd ~/event_horizon/deployments/k6

# Запускаем нагрузочный тест (10 виртуальных пользователей, 30 секунд)
k6 run --vus 10 --duration 30s e2e-test.js

# Если хочешь более жесткий тест (50 пользователей, 1 минута)
k6 run --vus 50 --duration 60s e2e-test.js
```

Что произойдет: Система начнет обрабатывать запросы, в базах появятся данные, а NATS начнет гонять события. Теперь метрики в Prometheus пойдут в гору.

## 📊 Фаза 2: Сбор метрик (Prometheus Queries)
Теперь, когда система "взбудоражена", давай снимем показатели. Вставь эти команды в новый терминал, пока k6 еще работает (или сразу после).

### 1. Базовые метрики (RPS, ошибки, латентность)
```bash
# --- RPS (Общее количество запросов в секунду) ---
# Суммарный RPS на все Gateway
curl -s "http://localhost:9090/api/v1/query?query=sum(rate(gateway_requests_total[1m]))" | jq '.data.result[0].value[1]'

# RPS на чтение (GET)
curl -s "http://localhost:9090/api/v1/query?query=sum(rate(gateway_requests_total{method='GET'}[1m]))" | jq '.data.result[0].value[1]'

# RPS на запись (POST)
curl -s "http://localhost:9090/api/v1/query?query=sum(rate(gateway_requests_total{method='POST'}[1m]))" | jq '.data.result[0].value[1]'

# --- ERROR RATE (Процент ошибок 5xx) ---
curl -s "http://localhost:9090/api/v1/query?query=sum(rate(gateway_requests_total{status=~'5..'}[1m])) / sum(rate(gateway_requests_total[1m])) * 100" | jq '.data.result[0].value[1]'

# --- LATENCY (P99) ---
# P99 latency для всех запросов
curl -s "http://localhost:9090/api/v1/query?query=histogram_quantile(0.99, sum(rate(gateway_request_duration_seconds_bucket[1m])) by (le))" | jq '.data.result[0].value[1]'
```

### 2. Go Runtime метрики (Goroutines, GC, Heap)
```bash
# --- Goroutines (Game сервис) ---
curl -s "http://localhost:9090/api/v1/query?query=go_goroutines{job='game'}" | jq '.data.result[0].value[1]'

# --- GC Cycles (Количество сборок мусора) ---
curl -s "http://localhost:9090/api/v1/query?query=rate(go_gc_cycles_automatic_gc_cycles_total{job='game'}[1m])" | jq '.data.result[0].value[1]'

# --- Heap (Используемая память в байтах, Game сервис) ---
curl -s "http://localhost:9090/api/v1/query?query=go_memstats_heap_alloc_bytes{job='game'}" | jq '.data.result[0].value[1]'

# --- Mutex Waits (Блокировки) ---
curl -s "http://localhost:9090/api/v1/query?query=rate(go_mutex_wait_total_seconds_total{job='game'}[1m])" | jq '.data.result[0].value[1]'
```

### 3. NATS Метрики (Событийная шина)
```bash
# --- Количество подключений к NATS ---
curl -s "http://localhost:9090/api/v1/query?query=gnatsd_varz_connections" | jq '.data.result[0].value[1]'

# --- Количество входящих сообщений в секунду (in_msgs) ---
curl -s "http://localhost:9090/api/v1/query?query=rate(gnatsd_varz_in_msgs[1m])" | jq '.data.result[0].value[1]'

# --- Количество исходящих сообщений в секунду (out_msgs) ---
curl -s "http://localhost:9090/api/v1/query?query=rate(gnatsd_varz_out_msgs[1m])" | jq '.data.result[0].value[1]'
```

### 4. WebSocket и Бизнес-метрики
```bash
# --- WebSocket соединения (если есть кастомная метрика) ---
# Пока нет, но смотрим на активные соединения через балансировщик
curl -s "http://localhost:9090/api/v1/query?query=balancer_active_connections" | jq '.data.result[0].value[1]'

# --- Количество отправленных рекордов (бизнес-метрика) ---
curl -s "http://localhost:9090/api/v1/query?query=sum(rate(game_submits_total[1m]))" | jq '.data.result[0].value[1]'
```

## 📈 Фаза 3: Визуальный осмотр (Grafana)
Если хочешь увидеть это все глазами, открой браузер и зайди в Grafana:

- URL: http://localhost:3000

- Логин/Пароль: admin / admin

Что посмотреть:

- Перейди в Explore (иконка лупы слева).

- Выбери источник данных Prometheus.

- Введи запрос gateway_request_duration_seconds и нажми Run. Ты увидишь гистограмму распределения времени ответа.

- Теперь попробуй rate(gateway_requests_total[1m]) — увидишь график RPS в реальном времени.

## 🛠️ Фаза 4: "Тупой" тест для проверки БД и Кеша
Этот тест проверит, работают ли Redis и PostgreSQL в связке. Он сначала запишет данные (в БД), а потом быстро прочитает их (из кеша).

```bash
# 1. Отправляем рекорд (запись в PG и публикация в NATS)
TOKEN=$(curl -s -X POST http://localhost:8079/api/auth/login -H "Content-Type: application/json" -d '{"email":"test@example.com","password":"secret123"}' | jq -r '.access_token')

curl -X POST http://localhost:8079/api/game/submit -H "Content-Type: application/json" -H "Authorization: Bearer $TOKEN" -d '{"user_id":"test-user","game_id":"hexagon","level":1,"score":999,"seed":"test_seed","moves":[]}'

# 2. Читаем лидерборд (Чтение из Redis кеша) — должно ответить мгновенно
time curl -s "http://localhost:8079/api/leaderboard?game_id=hexagon&limit=10" | jq '.'
```

# 🎯 Что ты получишь на выходе?

- Цифры по RPS, ошибкам, задержкам в реальном времени.

- Понимание, как твоя система ведет себя под нагрузкой (пусть и синтетической).

- Слепок Go-метрик, по которым можно понять, не течет ли память и не бесконечны ли горутины.

## SCRIPT

```bash
#!/bin/bash

# ============================================================
# Скрипт сбора метрик Event Horizon
# Использование: ./metrics_snapshot.sh [load_duration] [vus]
# Пример: ./metrics_snapshot.sh 30 10
# ============================================================

set -e

# --- Настройки ---
PROMETHEUS_URL="http://localhost:9090"
K6_DIR="./deployments/k6"
OUTPUT_DIR="./metrics_snapshots"
TIMESTAMP=$(date +"%Y%m%d_%H%M%S")
LOAD_DURATION=${1:-30}  # По умолчанию 30 секунд
VUS=${2:-10}            # По умолчанию 10 пользователей
OUTPUT_FILE="${OUTPUT_DIR}/snapshot_${TIMESTAMP}.json"

mkdir -p "$OUTPUT_DIR"

# --- Функция для запроса метрики из Prometheus ---
query_metric() {
    local query=$1
    local result=$(curl -s -G --data-urlencode "query=$query" "${PROMETHEUS_URL}/api/v1/query" | jq -r '.data.result[0].value[1] // "null"')
    echo "$result"
}

echo "🚀 Запуск сбора метрик Event Horizon"
echo "📅 Время: $(date)"
echo "⚙️  Нагрузка: ${VUS} пользователей, ${LOAD_DURATION} секунд"
echo "📁 Результат: ${OUTPUT_FILE}"
echo ""

# --- Фаза 1: Нагрузочный тест (в фоне) ---
echo "🔥 Запуск нагрузочного теста k6..."
cd "$K6_DIR"
k6 run --vus "$VUS" --duration "${LOAD_DURATION}s" e2e-test.js > /tmp/k6_output.log 2>&1 &
K6_PID=$!
cd - > /dev/null

# Даем системе время на разогрев перед сбором метрик
echo "⏳ Ожидание 5 секунд для прогрева системы..."
sleep 5

# --- Фаза 2: Сбор метрик ---
echo "📊 Сбор метрик из Prometheus..."

# 1. Базовые метрики
RPS=$(query_metric "sum(rate(gateway_requests_total[1m]))")
RPS_READ=$(query_metric "sum(rate(gateway_requests_total{method='GET'}[1m]))")
RPS_WRITE=$(query_metric "sum(rate(gateway_requests_total{method='POST'}[1m]))")
ERROR_RATE=$(query_metric "sum(rate(gateway_requests_total{status=~'5..'}[1m])) / sum(rate(gateway_requests_total[1m])) * 100")
LATENCY_P99=$(query_metric "histogram_quantile(0.99, sum(rate(gateway_request_duration_seconds_bucket[1m])) by (le))")

# 2. Go Runtime
GOROUTINES=$(query_metric "go_goroutines{job='game'}")
GC_CYCLES=$(query_metric "rate(go_gc_cycles_automatic_gc_cycles_total{job='game'}[1m])")
HEAP=$(query_metric "go_memstats_heap_alloc_bytes{job='game'}")
MUTEX=$(query_metric "rate(go_mutex_wait_total_seconds_total{job='game'}[1m])")

# 3. NATS
NATS_CONNS=$(query_metric "gnatsd_varz_connections")
NATS_IN=$(query_metric "rate(gnatsd_varz_in_msgs[1m])")
NATS_OUT=$(query_metric "rate(gnatsd_varz_out_msgs[1m])")

# 4. Бизнес-метрики
BALANCER_CONNS=$(query_metric "balancer_active_connections")
GAME_SUBMITS=$(query_metric "sum(rate(game_submits_total[1m]))")

# 5. Redis (если есть экспортер)
REDIS_MEMORY=$(query_metric "redis_memory_used_bytes{job='redis'}" || echo "null")
REDIS_CONNS=$(query_metric "redis_connected_clients{job='redis'}" || echo "null")

# 6. PostgreSQL (если есть экспортер)
PG_CONNS=$(query_metric "pg_stat_database_numbackends{datname='eventhorizon'}" || echo "null")
PG_DEADLOCKS=$(query_metric "pg_stat_database_deadlocks{datname='eventhorizon'}" || echo "null")

# Дожидаемся завершения k6
echo "⏳ Ожидание завершения нагрузочного теста..."
wait $K6_PID
echo "✅ Нагрузочный тест завершен."

# --- Фаза 3: Сохранение результатов в JSON ---
echo "💾 Сохранение результатов..."

cat > "$OUTPUT_FILE" << EOF
{
  "timestamp": "$(date -Iseconds)",
  "test_config": {
    "duration_seconds": $LOAD_DURATION,
    "vus": $VUS
  },
  "metrics": {
    "rps": {
      "total": $RPS,
      "read": $RPS_READ,
      "write": $RPS_WRITE,
      "error_rate_percent": $ERROR_RATE
    },
    "latency": {
      "p99_seconds": $LATENCY_P99
    },
    "go_runtime": {
      "goroutines": $GOROUTINES,
      "gc_cycles_per_second": $GC_CYCLES,
      "heap_alloc_bytes": $HEAP,
      "mutex_wait_seconds_per_second": $MUTEX
    },
    "nats": {
      "connections": $NATS_CONNS,
      "in_msgs_per_second": $NATS_IN,
      "out_msgs_per_second": $NATS_OUT
    },
    "business": {
      "balancer_active_connections": $BALANCER_CONNS,
      "game_submits_per_second": $GAME_SUBMITS
    },
    "storage": {
      "redis_memory_bytes": $REDIS_MEMORY,
      "redis_connections": $REDIS_CONNS,
      "postgres_connections": $PG_CONNS,
      "postgres_deadlocks": $PG_DEADLOCKS
    }
  }
}
EOF

echo ""
echo "✅ Готово!"
echo "📁 Результаты сохранены в: $OUTPUT_FILE"
echo ""
echo "📊 Для просмотра метрик в консоли, выполните:"
echo "   cat $OUTPUT_FILE | jq '.'"
echo ""
echo "📈 Для просмотра в Grafana, откройте: http://localhost:3000"
```

## 🚀 Как пользоваться скриптом

Сохрани скрипт в корне проекта как metrics_snapshot.sh.

Дай ему права на выполнение:

```bash
chmod +x metrics_snapshot.sh
chmod +x metrics_snapshot_dif.sh
```

Запусти с параметрами по умолчанию (30 секунд, 10 пользователей):

```bash
./metrics_snapshot.sh
```

Или с кастомной нагрузкой (60 секунд, 50 пользователей):

```bash
./metrics_snapshot.sh 60 50
```

## RESULTS

Отказано в доступе, потому что файл metrics_snapshot.sh не помечен как исполняемый. В Linux без этого скрипт не запустится.

#### 🛠️ Решение

Выполни одну команду, чтобы дать скрипту права на выполнение, а затем запусти его снова:

```bash
# Даём права на выполнение
chmod +x metrics_snapshot.sh

# Запускаем с нагрузкой 60 секунд и 50 пользователями
./metrics_snapshot.sh 60 50
```

#### 🚀 Если хочешь сначала прогнать короткий тест (для проверки)
```bash
# Быстрый тест: 30 секунд, 10 пользователей
./metrics_snapshot.sh 30 10
```

#### 📂 Что произойдет дальше
Скрипт запустит k6 в фоне (ты увидишь прогресс).

Подождет 5 секунд, пока система "прогреется".

Соберет все метрики из Prometheus.

Сохранит их в папку metrics_snapshots/ с именем типа snapshot_20250629_190000.json.

Сравнит с предыдущим снапшотом (если он есть) и покажет красивую таблицу с изменениями в процентах.

#### 💡 Совет на будущее
Если ты планируешь использовать этот скрипт часто, добавь его в PATH или создай алиас:

```bash
# Добавить в ~/.bashrc для постоянного использования
alias metrics='~/event_horizon/metrics_snapshot.sh'
```

Теперь ты можешь запускать тесты одной командой: metrics 60 50.

#### 🧠 Что дальше?

После того как скрипт отработает, ты получишь:

- JSON-файл с сырыми данными (можно использовать для отчетов).

- Красивую таблицу в консоли с изменениями.

- Понимание, как ведет себя система под нагрузкой.