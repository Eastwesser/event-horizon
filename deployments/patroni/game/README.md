# Patroni stub — Game Postgres HA

> **Status:** planned (copy [`../auth/`](../auth/) after Auth failover drill).

| Var | Example |
|-----|---------|
| `PATRONI_SCOPE` | `eh-game` |
| `DB_HOST` (when live) | `game-pg-haproxy` |
| Compose DB today | `postgres-game` :5461 |

See [`../../README.md`](../../README.md).
